/** 
 * PostCSS konfigurationsfil för fb-page-analyzer
 * Konfigurerar PostCSS plugins för CSS-processering (ES Module format)
 */
export default {
  plugins: {
    // Tailwind CSS integration
    tailwindcss: {},
    // Automatisk vendor prefix hantering
    autoprefixer: {},
  },
}
